local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local Window = Fluent:CreateWindow({
    Title = "Gemini Hub - Auto Farm",
    SubTitle = "Event-Based Logic",
    TabWidth = 160,
    Size = UDim2.fromOffset(480, 350),
    Acrylic = true,
    Theme = "Dark"
})

local Tabs = {
    FarmTab = Window:AddTab({ Title = "Main Farm", Icon = "rbxassetid://4483345998" }),
    SamTab = Window:AddTab({ Title = "Sam Quest", Icon = "rbxassetid://4483345998" })
}

-- [ VARIABLES & SERVICES ]
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer

local AutoFish = false
local FishBlock = nil
local AutoSam = false
local AutoClaimCompass = false

-- States để chống xung đột
local isWaitingForSam = false
local isDoingSam = false

getgenv()._safeTweenBusy = false
getgenv().TweenSettings = {
    MaxSpeed = 250,
    MinTweenTime = 0.2,
    MaxTweenTime = 15,
    RandomDelay = { Min = 0, Max = 0 }
}

-- [ CORE UTILITIES ]
local function charOk()
    local char = LocalPlayer.Character
    return char and char:FindFirstChild("Humanoid") and char:FindFirstChild("HumanoidRootPart") and char
end

local function SafeTween(goalPos, clickRadius)
    local char = charOk()
    if not char then return false end
    local hrp = char.HumanoidRootPart

    while getgenv()._safeTweenBusy do task.wait() end
    getgenv()._safeTweenBusy = true

    local distance = (hrp.Position - goalPos).Magnitude
    local cfg = getgenv().TweenSettings
    local speed = cfg.MaxSpeed or 250
    local tweenTime = math.clamp(distance / speed, cfg.MinTweenTime, cfg.MaxTweenTime)

    local tween = TweenService:Create(hrp, TweenInfo.new(tweenTime, Enum.EasingStyle.Linear), {
        CFrame = CFrame.new(goalPos)
    })
    tween:Play()
    tween.Completed:Wait()

    local rd = cfg.RandomDelay
    task.wait((rd.Min or 0) + math.random() * ((rd.Max or 0) - (rd.Min or 0)))

    if clickRadius and clickRadius > 0 then
        local parts = workspace:GetPartBoundsInRadius(goalPos, clickRadius)
        for _, part in ipairs(parts) do
            for _, cd in ipairs(part:GetDescendants()) do
                if cd:IsA("ClickDetector") then
                    fireclickdetector(cd, 1)
                end
            end
        end
    end

    getgenv()._safeTweenBusy = false
    return true
end

-- [ LOGIC: AUTO SAM (Bay 1200m -> Rớt 5 stud) ]
local function DoSamQuest(compass)
    local char = charOk()
    if not char then return end
    local hrp = char.HumanoidRootPart

    -- Equip compass
    compass.Parent = char
    local arrow = compass:WaitForChild("CompassArrow", 5)
    if not arrow then return end

    -- Tắt va chạm
    local originalCanCollide = {}
    for _, part in pairs(char:GetDescendants()) do
        if part:IsA("BasePart") then
            originalCanCollide[part] = part.CanCollide
            part.CanCollide = false
        end
    end

    local function restoreCollisions()
        for part, collide in pairs(originalCanCollide) do
            if part then part.CanCollide = collide end
        end
    end

    -- Khóa trọng lực
    local originalGravity = workspace.Gravity
    workspace.Gravity = 0
    local gravityConnection = workspace:GetPropertyChangedSignal("Gravity"):Connect(function()
        if workspace.Gravity ~= 0 then workspace.Gravity = 0 end
    end)

    local function resetGravity()
        if gravityConnection then gravityConnection:Disconnect() end
        workspace.Gravity = originalGravity
    end

    local diedConn = char:WaitForChild("Humanoid").Died:Connect(function()
        resetGravity()
        restoreCollisions()
        isDoingSam = false
    end)

    -- Kích hoạt la bàn
    if arrow.Transparency == 1 then
        compass:Activate()
    end
    local transConn = arrow:GetPropertyChangedSignal("Transparency"):Connect(function()
        if arrow.Transparency == 1 and char:FindFirstChild("Compass") then
            compass:Activate()
        end
    end)

    -- BƯỚC 1: BAY LÊN 1200 MÉT
    local skyY = 1200
    SafeTween(Vector3.new(hrp.Position.X, skyY, hrp.Position.Z))

    -- BƯỚC 2: DI CHUYỂN TRÊN TRỜI
    local stepDistance = 30
    local tweenTime = 0.2
    local repeatCount = 500
    local smoothFactor = 0.25
    local lastLookVector = nil
    local sharpTurnDetected = false
    local sharpTurnPos = nil

    local currentPos = hrp.Position

    for i = 1, repeatCount do
        if not AutoSam or sharpTurnDetected then break end
        if not char:FindFirstChild("Compass") then break end -- Mất la bàn
        if not arrow or not arrow:FindFirstChildWhichIsA("Weld") then break end

        local weld = arrow:FindFirstChildWhichIsA("Weld")
        local arrowCFrame = weld.Part0.CFrame * weld.C0 * weld.C1:Inverse() * CFrame.Angles(-math.rad(90), 0, 0)
        local rawLook = Vector3.new(arrowCFrame.LookVector.X, 0, arrowCFrame.LookVector.Z).Unit

        -- Detect sharp turn (Vượt qua mục tiêu)
        if lastLookVector then
            local dotProduct = math.clamp(lastLookVector:Dot(rawLook), -1, 1)
            local angleDiff = math.acos(dotProduct)
            if angleDiff > math.rad(120) then
                sharpTurnDetected = true
                sharpTurnPos = hrp.Position
                break
            end
        end

        local moveLook = rawLook
        if lastLookVector then
            moveLook = (lastLookVector * (1 - smoothFactor) + rawLook * smoothFactor).Unit
        end
        lastLookVector = rawLook

        -- Giữ nguyên Y = 1200
        currentPos = currentPos + (moveLook * stepDistance)
        currentPos = Vector3.new(currentPos.X, skyY, currentPos.Z)
        local destCFrame = CFrame.new(currentPos, currentPos + moveLook)

        local tween = TweenService:Create(hrp, TweenInfo.new(tweenTime, Enum.EasingStyle.Linear), {CFrame = destCFrame})
        tween:Play()
        tween.Completed:Wait()
    end

    -- BƯỚC 3 & 4: HẠ XUỐNG 5 STUD SO VỚI MẶT ĐẤT VÀ TÌM TREES SPAWNER
    if sharpTurnDetected and sharpTurnPos then
        local rayOrigin = Vector3.new(sharpTurnPos.X, skyY, sharpTurnPos.Z)
        local rayDirection = Vector3.new(0, -2000, 0)
        local rayParams = RaycastParams.new()
        rayParams.FilterDescendantsInstances = {char}
        rayParams.FilterType = Enum.RaycastFilterType.Exclude

        local raycastResult = workspace:Raycast(rayOrigin, rayDirection, rayParams)
        local groundY = 215 
        if raycastResult then
            groundY = raycastResult.Position.Y
        end

        SafeTween(Vector3.new(sharpTurnPos.X, groundY + 5, sharpTurnPos.Z))
        task.wait(0.2)

        -- Tìm Spawner (giới hạn thời gian hoặc lặp để tránh bị kẹt vô hạn)
        local attempts = 0
        while AutoSam and char:FindFirstChild("Compass") and attempts < 10 do
            attempts = attempts + 1
            for _, tree in pairs(workspace.Trees:GetChildren()) do
                if tree:FindFirstChild("Spawner") then
                    local spawner = tree.Spawner
                    local dist = (spawner.Position - hrp.Position).Magnitude
                    if dist < 100 then
                        hrp.CFrame = spawner.CFrame
                        task.wait(0.5)
                    end
                end
            end
            task.wait(0.5)
        end
    end

    -- Hoàn tất / Clean up
    if diedConn then diedConn:Disconnect() end
    if transConn then transConn:Disconnect() end
    resetGravity()
    restoreCollisions()
end

-- [ EVENT LOGIC: TRIGGER SAM SEQUENCE ]
local function TriggerClaimAndSam()
    -- Gửi lệnh Claim1 tới Server
    ReplicatedStorage:WaitForChild("Connections"):WaitForChild("Claim_Sam"):FireServer("Claim1")
    
    -- Nếu đang bật Sam và chưa trong chuỗi hoạt động nào của Sam, tiến hành chờ
    if AutoSam and not isWaitingForSam and not isDoingSam then
        task.spawn(function()
            isWaitingForSam = true
            Fluent:Notify({ Title = "Sam Quest", Content = "Đã gửi lệnh Claim! Đang chờ 20 giây...", Duration = 3 })
            
            task.wait(20) -- Chờ đủ 20 giây theo yêu cầu
            
            isWaitingForSam = false
            
            -- Sau 20s, kiểm tra xem có nhận được la bàn không
            local char = charOk()
            local compass = LocalPlayer.Backpack:FindFirstChild("Compass") or (char and char:FindFirstChild("Compass"))
            
            if compass then
                isDoingSam = true
                Fluent:Notify({ Title = "Sam Quest", Content = "Đã thấy La bàn! Bắt đầu bay...", Duration = 3 })
                DoSamQuest(compass)
                isDoingSam = false
            end
        end)
    end
end

-- [ LOGIC: AUTO FISH ]
local function DoFishQuest()
    if not FishBlock then return end
    local char = charOk()
    if not char then return end
    
    local hrp = char.HumanoidRootPart
    local targetPos = FishBlock.Position + Vector3.new(0, 3, 0)

    if (hrp.Position - targetPos).Magnitude > 5 then
        SafeTween(targetPos, 0)
    end

    for _, tool in pairs(LocalPlayer.Backpack:GetChildren()) do
        if tool:IsA("Tool") and string.find(tool.Name, "Rod") then
            char.Humanoid:EquipTool(tool)
            break
        end
    end

    local rod
    for _, tool in pairs(char:GetChildren()) do
        if tool:IsA("Tool") and string.find(tool.Name, "Rod") and tool:FindFirstChild("Click") then
            rod = tool
            break
        end
    end
    if not rod then return end

    local bobber
    for _, desc in pairs(rod:GetDescendants()) do
        if desc.Name == "Bobber" and desc:FindFirstChild("Effect") then
            bobber = desc
            break
        end
    end

    if not bobber then
        rod.Click:FireServer(hrp.Position)
        return
    end

    if bobber.Effect.Enabled then
        task.wait(0.6)
        rod.Click:FireServer(hrp.Position)

        local retum = workspace:FindFirstChild("Merchants")
            and workspace.Merchants:FindFirstChild("FishMerchant")
            and workspace.Merchants.FishMerchant:FindFirstChild("Clickable")
            and workspace.Merchants.FishMerchant.Clickable:FindFirstChild("Retum")

        if retum then
            local npc = workspace.Merchants.FishMerchant
            local npcHrp = npc:FindFirstChild("HumanoidRootPart")
            if npcHrp then
                local pos = npcHrp.Position + (npcHrp.CFrame.LookVector * -3)
                pos = Vector3.new(pos.X, npcHrp.Position.Y, pos.Z)
                SafeTween(pos, 0)
                hrp.CFrame = CFrame.new(hrp.Position, npcHrp.Position)
                task.wait(0.2)
                retum:FireServer()
            end
        end
    end
end

-- [ MASTER LOOP ]
task.spawn(function()
    while task.wait(0.5) do
        pcall(function()
            -- CHỈ thực hiện Auto Fish khi không bận chờ/làm Sam
            if not isWaitingForSam and not isDoingSam then
                if AutoFish then
                    DoFishQuest()
                end
            end
        end)
    end
end)

-- [ AUTO CLAIM COMPASS LOOP ]
task.spawn(function()
    while task.wait(5) do
        if AutoClaimCompass then
            pcall(function()
                TriggerClaimAndSam()
            end)
        end
    end
end)

-- [ UI TABS SETUP ]
Tabs.FarmTab:AddToggle("ToggleAutoFish", {
    Title = "Auto Fish Farm",
    Description = "Dừng lại khi đang làm event Sam",
    Default = false,
    Callback = function(state)
        AutoFish = state
        if AutoFish and not FishBlock then
            FishBlock = Instance.new("Part")
            FishBlock.Name = "FishFarmPlatform"
            FishBlock.Anchored = true
            FishBlock.Size = Vector3.new(2.5, 1, 2.5)
            FishBlock.Material = Enum.Material.Plastic
            FishBlock.BrickColor = BrickColor.new("Medium stone grey")
            FishBlock.CFrame = CFrame.new(-20000, 211, 20000)
            FishBlock.Parent = workspace
        elseif not AutoFish and FishBlock then
            FishBlock:Destroy()
            FishBlock = nil
        end
    end
})

Tabs.SamTab:AddToggle("ToggleCompassFarm", {
    Title = "Auto Find Compass (Event Based)",
    Description = "Làm chuỗi bay cao tìm Sam nếu Claim1 thành công",
    Default = false,
    Callback = function(state)
        AutoSam = state
    end
})

Tabs.SamTab:AddToggle("AutoClaimCompass", {
    Title = "Auto Claim Compass",
    Description = "Tự động kích hoạt chuỗi Event 20s",
    Default = false,
    Callback = function(state)
        AutoClaimCompass = state
    end
})

local Section = Tabs.SamTab:AddSection("Manual Utilities")
Tabs.SamTab:AddButton({
    Title = "Claim One Compass",
    Callback = function()
        -- Kích hoạt bằng nút thủ công cũng sẽ vào chuỗi 20s nếu đang bật Auto Sam
        TriggerClaimAndSam()
    end
})
Tabs.SamTab:AddButton({
    Title = "Claim All Compasses",
    Callback = function()
        ReplicatedStorage:WaitForChild("Connections"):WaitForChild("Claim_Sam"):FireServer("Claim10")
    end
})

Fluent:Notify({ Title = "Gemini Hub", Content = "Đã cập nhật Event-Based Logic thành công!", Duration = 5 })
